// src/api/config.js

// URL del Gateway
export const API_BASE_URL = 'https://api-gateway.bim2.xyz/api/v1';

// En la Landing Page NO necesitamos token para registrar cocinas (según tu JSON),
// así que solo exportamos la URL base.